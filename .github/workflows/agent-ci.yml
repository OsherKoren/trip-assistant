name: Agent Service CI

on:
  push:
    branches:
      - main
      - dev
      - feature/agent
    paths:
      - 'agent/**'
      - '.github/workflows/agent-ci.yml'
  pull_request:
    paths:
      - 'agent/**'
      - '.github/workflows/agent-ci.yml'

env:
  AGENT_DIR: ./agent

jobs:
  # ========================================
  # JOB 0: Detect Changes
  # ========================================
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      agent: ${{ steps.filter.outputs.agent }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for agent changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            agent:
              - 'agent/**'

  # ========================================
  # JOB 1: Unit Tests (Fast, Mocked, Always Run)
  # ========================================
  unit-tests:
    name: Unit Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.agent == 'true'
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13']
    defaults:
      run:
        working-directory: ${{ env.AGENT_DIR }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Run unit tests
        run: pytest tests/ -v -m "not integration" --cov=src --cov-report=xml --cov-report=term | tee coverage.txt

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          files: ./agent/coverage.xml
          flags: unittests
          name: agent-unit-tests
        if: always()

      - name: Update coverage badge
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && matrix.python-version == '3.13'
        run: |
          COV=$(grep '^TOTAL' coverage.txt | awk '{print $NF}' | tr -d '%')
          if (( $(echo "$COV < 70" | bc -l) )); then COLOR="red"
          elif (( $(echo "$COV < 80" | bc -l) )); then COLOR="orange"
          elif (( $(echo "$COV < 90" | bc -l) )); then COLOR="yellow"
          else COLOR="brightgreen"; fi
          echo "{\"schemaVersion\":1,\"label\":\"agent coverage\",\"message\":\"${COV}%\",\"color\":\"${COLOR}\"}" > $GITHUB_WORKSPACE/badges/agent-coverage.json

      - name: Commit coverage badge
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && matrix.python-version == '3.13'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update agent coverage badge [skip ci]"
          file_pattern: badges/agent-coverage.json

  # ========================================
  # JOB 2: Integration Tests (Real API, Main Branch Only)
  # ========================================
  integration-tests:
    name: Integration Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: [changes, unit-tests]
    # Only run after merge to main, and only if agent files changed
    if: needs.changes.outputs.agent == 'true' && github.event_name == 'push' && github.ref == 'refs/heads/main'
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13']
    defaults:
      run:
        working-directory: ${{ env.AGENT_DIR }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Run integration tests
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: pytest tests/integration/ -v --tb=short

      - name: Report API costs
        if: always()
        run: |
          echo "Integration tests completed. Check OpenAI usage for costs (~\$0.01-0.10)"

  # ========================================
  # JOB 3: Code Quality Checks
  # ========================================
  quality-checks:
    name: Code Quality (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.agent == 'true'
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13']
    defaults:
      run:
        working-directory: ${{ env.AGENT_DIR }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Run Ruff linter
        run: ruff check src/ tests/

      - name: Run Ruff formatter
        run: ruff format --check src/ tests/

      - name: Run MyPy type checker
        run: mypy src/

